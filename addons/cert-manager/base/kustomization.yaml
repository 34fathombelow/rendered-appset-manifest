apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared, non-chart resources only. The Helm chart itself is inflated by each
# overlay so that every environment can pin its own values file -- inflating it
# here as well would render the chart twice.
resources:
  - cluster-issuer.yaml
